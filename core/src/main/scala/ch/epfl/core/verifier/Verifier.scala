package ch.epfl.core.verifier

import ch.epfl.core.models.application.ApplicationLibrary

object Verifier {
  def verify(newAppsLibrary: ApplicationLibrary, existingAppsLibrary: ApplicationLibrary): ApplicationLibrary = {
    existingAppsLibrary
  }
}
